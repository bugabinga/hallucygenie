# Autoresearch Ideas

- Add non-quota tests for latest MiniMax docs/OpenAPI model enums and request payload shapes generated from cached docs.
- Consider explicit `language_boost: "auto"` for TTS only if real smoke test proves better reliability without latency harm.
- Consider using `GET /v1/get_voice` in manual smoke docs to validate default voice, but do not call in normal tests.
